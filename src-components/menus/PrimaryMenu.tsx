import { React } from 'src-core/react';

import { css } from 'src-core/style';

export const PrimaryMenu = () => (
  <div>
    <h2
      {...css({
        padding: 10,
      })}>
      PrimaryMenu
    </h2>
  </div>
);
